# SECRET_KEY = "132abc"

# ALLOWED_HOSTS = ["*"]

# INSTALLED_APPS = ["django_json_middleware"]

# DATABASES = {"default": {"ENGINE": "django.db.backends.sqlite3"}}

# ROOT_URLCONF = "tests.urls"

# MIDDLEWARE = ["django_json_middleware.middleware"]
